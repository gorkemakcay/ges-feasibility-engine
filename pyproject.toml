[project]
name = "gesfeas"
version = "0.1.0"
description = "GES Fizibilite Engine"
requires-python = ">=3.11"
dependencies = [
    "pvlib",
    "nrel-pysam",
    "pandas",
    "pydantic",
    "pyyaml",
    "requests",
    "pytest"
]
